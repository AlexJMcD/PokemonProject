# PokemonProject
This branch was created once all the basic logic for converting the JSON data had been handled. From this point on features of the app will only use specific parts
of that data, so if I wish to return and create an alternate version of the app that uses different data, then this is a good base point.
